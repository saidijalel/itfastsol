{% extends 'theme/theme1/layout.html.twig' %}

{% block body %}

    <div class="row">
        <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">

            <!-- Indicators -->
            <ol class="carousel-indicators">
                {% for product in products %}
                <li data-target="#carousel-example-generic" data-slide-to="{{ loop.index }}" {% if loop.index == 1 %}class="active" {% endif %}"></li>
                {% endfor %}
            </ol>

            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                {% for product in products %}
                <div class="item {% if loop.index == 1 %}active{% endif %}">
                    <img src="{{ asset('uploads') }}/{{ product.photo }}" alt="..." style="height:300px" class="center-block">
                    <div class="carousel-caption">
                        <h3> {{ product.title }}</h3>
                        <p>{{ product.description[:50] ~ '...' }}</p>
                    </div>
                </div>
                {% endfor %}
            </div>

            <!-- Controls -->
            <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>

    </div>






    <div class="row">
        <div class="col-md-6">
             <div class="panel panel-default top-buffer">
                    <div class="panel-heading">News</div>
                    <div class="panel-body">
                        <ul class="media-list">
                                    {% for post in posts %}
                                        <li class="media">
                                            <div class="media-left">
                                                <a href="#">
                                                    <img class="media-object" src="http://lorempixel.com/100/100/" alt="...">
                                                </a>
                                            </div>
                                            <div class="media-body">
                                                <h4 class="media-heading"> <a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.title }}</a> </h4>
                                                <p>{{ post.body }}</p>
                                            </div>
                                        </li>
                                    {% endfor %}
                                </ul>
                    </div>
                </div>

                <div class="panel panel-default top-buffer">
                    <div class="panel-heading">News 2</div>
                    <div class="panel-body">
                        <ul class="media-list">
                                    {% for post in posts %}
                                        <li class="media">
                                            <div class="media-left">
                                                <a href="#">
                                                    <img class="media-object" src="http://lorempixel.com/100/100/" alt="...">
                                                </a>
                                            </div>
                                            <div class="media-body">
                                                <h4 class="media-heading"> <a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.title }}</a> </h4>
                                                <p>{{ post.body }}</p>
                                            </div>
                                        </li>
                                    {% endfor %}
                                </ul>
                    </div>
                </div>

        </div>


         <div class="col-md-6">
            <div class="panel panel-default top-buffer">
                    <div class="panel-heading">Top Products</div>
                    <div class="panel-body">

                            <ul class="media-list">
                                    {% for product in products %}
                                        <li class="media">
                                            <div class="media-left">
                                                <a href="#">
                                                    <img class="media-object img-thumbnail" src="{{ asset('uploads') }}/{{ product.photo }}" alt="..." style="width: 100px">
                                                </a>
                                            </div>
                                            <div class="media-body">
                                                <h4 class="media-heading"> {{ product.title }}</h4>
                                                <p>{{ product.description[:50] ~ '...' }}</p>
                                            </div>
                                        </li>
                                    {% endfor %}
                                </ul>
                    </div>
                </div>
        </div>

    </div>



{% endblock %}


